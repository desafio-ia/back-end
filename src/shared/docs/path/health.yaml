/health:
  get:
    summary: Health check
    tags:
      - Health
    responses:
      200:
        description: OK
        content:
          application/json:
            schema:
              $ref: '../schemas/health.schema.yaml'